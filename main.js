let myName = prompt('Ismingizni kiriting');
let myId = prompt('Space idingizni kiriting');
let myCode = prompt('Space psswordingizni kiriting');
const acc = {
    name: myName,
    id: myId,
    password: myCode
}

console.log(acc);